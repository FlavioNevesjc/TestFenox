@model IEnumerable<TestFenox.Models.Fuel>

@{
    ViewData["Title"] = "Combustivel";
}

<h1>Combustivel</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdFuel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescriptionFuel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StatusFuel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdFuel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescriptionFuel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StatusFuel)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
